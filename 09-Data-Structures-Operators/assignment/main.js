// Main entry point
// Books import (Named Import)
import { books } from "./books-data.js";

// console.log("Books data loaded:", books);

// Destructuring Arrays (Side-effect Import) - (Importing a module JUST to run its code, not to use its exports.)
import "./array-destructuring.js";

// Destructuring Object (Side-effect Import) - (Importing a module JUST to run its code, not to use its exports.)
import "./object-destructuring.js";
